# Plotter 绘图工具

## 功能简介

通过 UDP 协议发送 JSON 格式的数据，用于实时数据可视化。

## 主要功能

- **UDP 通信**：通过 UDP socket 发送数据到指定主机和端口
- **JSON 格式**：支持发送任意 JSON 格式的数据
- **线程安全**：使用互斥锁保证多线程环境下的安全性

## 使用说明

- 初始化时可指定目标主机地址和端口（默认：127.0.0.1:9870）
- 通过 `plot(json)` 方法发送 JSON 数据
- 数据会被序列化为 JSON 字符串并通过 UDP 发送

