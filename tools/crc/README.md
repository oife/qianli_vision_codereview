# CRC 校验工具

## 功能简介

提供 CRC（循环冗余校验）计算和校验功能，支持 CRC8 和 CRC16 两种校验方式。

## 主要功能

- **CRC8 计算和校验**：计算数据的 CRC8 校验值，或验证包含 CRC8 的数据完整性
- **CRC16 计算和校验**：计算数据的 CRC16 校验值，或验证包含 CRC16 的数据完整性

## 使用说明

- `get_crc8()` / `get_crc16()`：计算校验值（长度不包括校验位）
- `check_crc8()` / `check_crc16()`：校验数据完整性（长度包括校验位）

