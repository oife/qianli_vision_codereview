# Image Tools 图像工具

## 功能简介

提供基于 OpenCV 的图像绘制工具函数，用于在图像上绘制点、文本等可视化元素。

## 主要功能

- **绘制点**：在图像上绘制单个点（支持自定义颜色和半径）
- **绘制点集**：批量绘制多个点（支持 `cv::Point` 和 `cv::Point2f`）
- **绘制文本**：在图像上绘制文本标签（支持自定义颜色、字体大小和粗细）

## 使用说明

所有函数直接操作输入的 `cv::Mat` 图像对象，支持自定义颜色、大小等参数。

