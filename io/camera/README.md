# Camera 模块

相机模块提供了统一的相机接口抽象，用于封装不同厂商的相机驱动。

## 功能

- 定义了 `CameraBase` 抽象基类，提供统一的相机读取接口
- 实现了 `Camera` 类，作为相机工厂，根据配置自动选择并初始化相应的相机驱动
- 支持多种相机驱动的统一访问接口

## 主要类

- `CameraBase`: 相机基类，定义了 `read()` 虚函数接口
- `Camera`: 相机工厂类，根据配置文件选择并封装具体的相机实现

## 使用方式

通过配置文件初始化相机，然后调用 `read()` 方法获取图像和时间戳。

