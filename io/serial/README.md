# Serial 模块

串口通信库，提供跨平台的串口通信功能。

## 功能

- 跨平台串口通信支持（Linux/Windows）
- 串口打开、关闭、读写操作
- 串口参数配置（波特率、数据位、停止位、校验位等）
- 超时控制

## 说明

这是一个第三方串口通信库，被其他模块（如 `gimbal`、`dm_imu`）使用进行串口通信。

## 使用场景

- 云台通信（gimbal模块）
- IMU传感器通信（dm_imu模块）
- 其他需要串口通信的设备

